# Copy this to .env and edit values.
# Keep this file out of version control (.gitignore) and set permissions to 600.

# ── Container discovery ─────────────────────────────────────────────────────────
# Prefer finding the MySQL container by a published port. If not found, fall back
# to Swarm stack labels.
MYSQL_PUBLISHED_PORT=33066
MYSQL_INTERNAL_PORT=3306

# ── Credentials (use a least-privileged user when possible) ─────────────────────
MYSQL_USER=root_user
MYSQL_PASSWORD=change_me_very_strong

# ── Swarm fallback discovery ────────────────────────────────────────────────────
STACK_NS=dwdm
SERVICE_NAME=mysql

# ── Backup output & performance ─────────────────────────────────────────────────
# Where tar.gz backups are written
BACKUP_ROOT=/var/backups/mysql

# Parallel table dumps per DB during backup (1 = sequential)
JOBS=4
